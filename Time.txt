sudo apt-get install git  && curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc |  tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null && echo "deb https://ngrok-agent.s3.amazonaws.com buster main" |  tee /etc/apt/sources.list.d/ngrok.list &&  apt update &&  apt install ngrok && apt-get install golang-go && go install github.com/tomnomnom/waybackurls@latest&& sudo apt-get install net-tools && apt-get install console-common && cat TimeMachine.txt && cd && cd && go/bin/./waybackurls -h
